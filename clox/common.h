#ifndef clox_common_h
#define clox_common_h

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>

#define UINT8_COUNT (UINT8_MAX + 1)

/* Enable GC stress and logging for diagnostics while testing. Uncomment to
   re-enable. */
// #define DEBUG_STRESS_GC
// #define DEBUG_LOG_GC

#endif
